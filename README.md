# News Fetcher (Python)

A simple Python program that fetches the latest news articles based on user input using NewsAPI.

## What this does
The program asks the user for a topic and returns:
- Article titles
- URLs to read full articles

## Requirements

Install requests:
```bash
pip install requests
